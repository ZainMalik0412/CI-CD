name: Test custom action

on:
  push:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run local custom action
        uses: ./coderco-custom
        with:
          who-to-greet: Zain
